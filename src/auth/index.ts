/**
 * Bundled auth entry point for the self-contained root distribution.
 *
 * Keep this source re-export relative to the workspace so generated declarations
 * and JavaScript contain the implementation rather than a registry dependency.
 */
export * from '../../packages/auth/src/index';
